# Rate Confirmation Generator
# No environment variables required — this is a fully stateless, client-side tool.
# All data is stored in the browser (localStorage).

# Optional: Next.js telemetry opt-out
NEXT_TELEMETRY_DISABLED=1
